import React from 'react'


const List = (props) => {
    return (
        <ul>
            const mappedNames = {props.names}((name, i) => <h1> {name} </h1>)
        </ul>
    )
}






export default List